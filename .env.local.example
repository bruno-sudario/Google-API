# Copie para .env.local e preencha com a sua chave do Google Cloud.
# A chave deve ter restrição por referenciador HTTP e por API
# (Places API New, Maps JavaScript API, Geocoding API).
VITE_GOOGLE_MAPS_API_KEY=SUA_CHAVE_AQUI
